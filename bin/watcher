#!/usr/bin/env ruby

require 'bundler/setup'

project_root = File.join(File.dirname(__FILE__), '..')

$LOAD_PATH << File.join(project_root, 'lib')

require 'inews_client'

exec = ""
exec = ARGF.argv.first if ARGF.argv

load File.join(project_root, exec)
